{{- if include "langgraph-agent.createSecret" . }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "langgraph-agent.secretName" . }}
  labels:
    {{- include "langgraph-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  llm-api-key: {{ .Values.llm.apiKey | quote }}
{{- end }}

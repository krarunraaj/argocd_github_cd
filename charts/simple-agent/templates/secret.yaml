{{- if include "simple-agent.createSecret" . }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "simple-agent.secretName" . }}
  labels:
    {{- include "simple-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  llm-api-key: {{ .Values.llm.apiKey | quote }}
{{- end }}
